#version 400
layout (location = 0) in vec3 inPosition;

uniform mat4 LightSpaceMatrix;
uniform mat4 WorldMatrix;

void main()
{
    gl_Position = LightSpaceMatrix * WorldMatrix * vec4(inPosition, 1);
}